describe("Mongo", () => {
  beforeEach(() => {
  });
  
  it("should create an index from provided schema", async () => {
  });

  it("should create materialised views for immutable collections", async () => {
  });

  it("should generate query from AST extracting all details", async () => {
  });
});
