describe("Queries", () => {
  beforeEach(() => {
  });
  
  it("should wrap graphql resolver to return a single entity", async () => {
  });

  it("should wrap a graphql resolver to return multiple entities", async () => {
  });
});
