describe("Config", () => {
  beforeEach(() => {
  });
  
  it("should allow syncs to be added to global sync store", async () => {
  });

    
  it("should allow syncs to be deleted from global sync store", async () => {
  });
});
