describe("Promises", () => {
  beforeEach(() => {
  });
  
  it("should enqueue promises on to global call stack", async () => {
  });

  it("should enqueue promises on to provided call stack", async () => {
  });

  it("should process promises in global call stack", async () => {
  });

  it("should process promises in provided call stack", async () => {
  });

  it("should cleanup the provided call stack after processing if cleanup=true", async () => {
  });

  it("should preserve the provided call stack after processing if cleanup=false", async () => {
  });
});
