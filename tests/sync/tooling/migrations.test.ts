describe("Migrations", () => {
  beforeEach(() => {
  });
  
  it("should enqueue migration in callstack at position", async () => {
  });

  it("should enqueue migration at the end of callstack when using blockNumber=latest", async () => {
  });
});
