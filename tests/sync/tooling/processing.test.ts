describe("Processing", () => {
  beforeEach(() => {
  });
  
  it("should process a collection of operations using queryFilters", async () => {
  });

  it("should process new block enqueued via listener", async () => {
  });
});
