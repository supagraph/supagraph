describe("Fetch", () => {
  beforeEach(() => {
  });
  
  it("should gather blocks for events between blocks", async () => {
  });

  it("should gather transactions for events between blocks", async () => {
  });
});
